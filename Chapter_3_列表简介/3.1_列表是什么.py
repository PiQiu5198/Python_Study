#列表（list）由一系列按特定顺序排列的元素组成。
#你不仅可以创建包含字母表中所有字母、数字0~9或所有家庭成员的列表。
#还可以把任何东西加入列表，其中元素之间可以没有任何关系。
#列表通常包含多个元素，因此给列表指定一个表示复数的名称（如letters、digits或names）。

#在Python中，用方括号（[]）表示列表，用逗号分隔其中的元素。
bicycles = ['trek', 'cannondale', 'redline', 'specialized']
print(bicycles)
#鉴于这不是你要用户看到的输出，下面来学习如何访问列表元素。