message = "Hello Python World!"
print(message)
#我们添加了一个名为message的变量（variable）。
#每个变量指向一个值（Value）——与该变量相关联的信息。

message = "Hello Python Crash CourseWorld!"
print(message)
#在程序中，可随时修改变量的值，而Python将始终记录变量的最新值。